name: intellihire
description: "Intelligent Recruitment & Skill Assessment System"

publish_to: "none"

version: 1.0.0

environment:
  sdk: ^3.8.1

dependencies:
  dynamic_color: ^1.8.1
  firebase_auth: ^6.0.1
  firebase_core: ^4.0.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
